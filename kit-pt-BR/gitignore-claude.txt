# ~/.claude/.gitignore — versiona SÓ a superfície de defesa e as memórias (tamper-evident).
# Tudo o mais (transcrições, caches, tokens) fica de fora — nunca suba este repo para a nuvem.
*
!.gitignore
!settings.json
!CLAUDE.md
!hooks/
!hooks/**
!agents/
!agents/**
!commands/
!commands/**
!skills/
!skills/**
!projects/
!projects/*/
!projects/*/memory/
!projects/*/memory/**
